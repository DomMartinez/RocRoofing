{
  "name": "rocroofing-webapp",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no tests have been built -- why not create one? :)\" && exit 1",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "npm run prebuild && tsc",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-ses": "^3.163.0",
    "dd-trace": "^2.3.0",
    "express": "^4.17.3",
    "express-prom-bundle": "^6.4.1",
    "prom-client": "^14.0.1"
  },
  "devDependencies": {
    "@aws-sdk/types": "^3.55.0",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.13",
    "tslint": "^6.1.3",
    "typescript": "^4.5.5"
  }
}
